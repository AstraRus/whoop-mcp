/**
 * Registry tools: per-day detail and data export.
 * Filled in by package P5 (get_day, export_health_data); empty until then.
 */

import type { AnyToolDefinition } from "../tool-definition.js";

/** Registry tools for per-day detail and data export. */
export const DAY_TOOLS: readonly AnyToolDefinition[] = [];
