/**
 * Registry tools: sync and server status.
 * Filled in by package P3 (get_sync_status); empty until then.
 */

import type { AnyToolDefinition } from "../tool-definition.js";

/** Registry tools for sync and server status. */
export const STATUS_TOOLS: readonly AnyToolDefinition[] = [];
