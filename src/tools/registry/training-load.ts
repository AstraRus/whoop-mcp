/**
 * Registry tools: training load and sport breakdown.
 * Filled in by package P6; empty until then.
 */

import type { AnyToolDefinition } from "../tool-definition.js";

/** Registry tools for training load and sport breakdown. */
export const TRAINING_LOAD_TOOLS: readonly AnyToolDefinition[] = [];
